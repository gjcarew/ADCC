<h1> <%=@team.name%> Competitors </h1>
<%@competitors.each do |c|%>
  <h2><%= c.name %></h2>
  <p>Weight class: <%= c.weight_class == 100 ? "99+" : c.weight_class%></p>
  <p><%=c.name%> has <%=c.previous_winner ? "" : "not"%> won ADCC before.</p>
<%end%>
